<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de Bord Professeur</title>
    <link rel="stylesheet" href="styles.css"> <!-- Lien vers ton fichier CSS -->
    <style>
        /* Inclusion des styles de l'espace étudiant */
        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f5f7fb;
            color: #333;
        }

        /* Sidebar */
        .sidebar {
            width: 80px;
            background-color: #333;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            padding: 20px;
            box-shadow: 2px 0 5px rgba(0,0,0,0.1);
            color: #fff;
            overflow: hidden;
        }

        .sidebar ul {
            list-style: none;
            padding: 0;
            width: 100%;
        }

        .sidebar ul li {
            margin: 10px 0;
            width: 100%;
        }

        .sidebar ul li a {
            text-decoration: none;
            color: #fff;
            font-size: 1em;
            display: flex;
            align-items: center;
            padding: 10px;
            transition: background-color 0.3s ease;
            box-sizing: border-box;
            overflow: hidden;
        }

        .sidebar ul li a:hover {
            background-color: #444;
            border-radius: 5px;
        }

        .sidebar ul li a span {
            margin-left: 10px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* Horizontal Menu */
        .horizontal-menu {
            background-color: #ffffff;
            padding: 10px 20px;
            border-radius: 15px;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
            margin-bottom: 20px;
            display: flex;
            justify-content: center;
        }

        .horizontal-menu ul {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
        }

        .horizontal-menu ul li {
            margin-right: 20px;
        }

        .horizontal-menu ul li a {
            text-decoration: none;
            color: #333;
            font-size: 1.2em;
            padding: 5px 10px;
            border-radius: 10px;
            transition: background-color 0.3s ease;
        }

        .horizontal-menu ul li a:hover {
            background-color: #f0f4ff;
        }

        /* Main Content */
        .main-content {
            margin-left: 120px;
            padding: 20px;
        }

        /* Header */
        header {
            display: flex;
            justify-content: flex-end;
            align-items: center;
            margin-bottom: 40px;
        }

        .header-actions {
            display: flex;
            align-items: center;
        }

        .header-actions button {
            background: none;
            border: none;
            font-size: 1.5em;
            margin-left: 20px;
            cursor: pointer;
        }

        .user-profile {
            display: flex;
            align-items: center;
            margin-left: 20px;
        }

        .user-profile img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            margin-right: 10px;
        }

        /* Projects Overview */
        .projects-overview {
            background-color: #ffffff;
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 40px;
            margin-left: 130px;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
        }

        .projects-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .projects-header h2 {
            font-size: 1.5em;
            margin: 0;
        }

        .projects-list {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .project-card {
            background-color: #f0f4ff;
            border-radius: 10px;
            padding: 20px;
            flex: 1;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
            max-width: 48%;
        }

        .project-card .project-date {
            font-size: 0.9em;
            margin-bottom: 10px;
            color: #888;
        }

        .project-card .project-title {
            font-size: 1.2em;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .project-card .project-subtitle {
            font-size: 1em;
            color: #555;
        }

        /* Important Notifications */
        .client-messages {
            background-color: #ffffff;
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
            margin-top: 240px;
            margin-left: 130px;
        }

        .client-messages h2 {
            font-size: 1.5em;
            margin-bottom: 20px;
        }

        .message-card {
            background-color: #f9f9f9;
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 20px;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.05);
        }

        .message-card p {
            margin: 0;
            margin-bottom: 10px;
        }

        .message-date {
            font-size: 0.9em;
            color: #888;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .main-content {
                margin-left: 0;
                padding: 10px;
            }

            .sidebar {
                width: 60px;
            }

            .sidebar ul li a span {
                display: none;
            }

            .projects-list {
                flex-direction: column;
            }

            .image-link-section {
            position: relative;
            margin-left: 120px;
            margin-top: 20px;
            border-radius: 15px;
            overflow: hidden;
            height: 150px !important; /* Force la hauteur */
            max-height: 150px !important; /* Force la hauteur maximale */
            width: calc(100% - 140px); /* Ajuste la largeur en tenant compte de la marge */
        }
    }
        .image-link {
            display: block;
            position: relative;
            width: 100%;
            height: 100%;
        }

        .image-background {
            width: 100%;
            height: 100% !important; /* Force la hauteur */
            max-height: 250px !important; /* Force la hauteur maximale */
            object-fit: cover;
            object-position: center;
        }

        .image-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.6);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: #fff;
            text-align: center;
            padding: 10px;
        }

        .image-overlay h2 {
            font-size: 1.5em;
            margin: 0;
            line-height: 1.2;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .image-overlay p {
            font-size: 1em;
            margin: 5px 0 0;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
        }
    </style>
</head>
<body>
<!-- Sidebar -->
<aside class="sidebar">
    <div class="logo">Smartify</div>
    <ul>
        <li><a href="/modifier-profil-professeur"><i class="icon-user"></i><span>Profil</span></a></li>
        <li><a href="/courses"><i class="icon-book"></i><span>Cours</span></a></li>
        <li><a href="/add-video-course"><i class="icon-video"></i><span>Ajouter Vidéo</span></a></li>
        <li><a href="/quizzes"><i class="icon-question"></i><span>Quizz</span></a></li>
        <li><a href="/messages"><i class="icon-envelope"></i><span>Messages</span></a></li>
        <form id="logoutForm" action="/logout" method="get" style="display: inline;">
            <button type="submit">Déconnexion</button>
        </form>
    </ul>
</aside>

<!-- Barre de menu horizontale -->
<nav class="horizontal-menu">
    <ul>
        <li><a href="/userSpace">Tableau de Bord</a></li>
        <li><a href="/courses">Cours</a></li>
        <li><a href="/add-video-course">Cours Vidéo</a></li>
        <li><a href="/add-quiz">Quizz</a></li>
        <div id="user">Bonjour <%= username %></div>
    </ul>
    
</nav>



<section class="image-link-section" style="margin-left: 120px;">
    <a href="/nouvelle-page" class="image-link">
        <img src="/images/exam2.jpg" alt="Étudiants passant un examen" class="image-background" style="opacity: 0.4;">
        <div class="image-overlay">
            <h2>Générez des examens automatiquement</h2>
            <p>Cliquez ici pour en savoir plus</p>
        </div>
    </a>
</section>


    <!-- Section Messages importants -->
    <section class="client-messages">
        <h2>Messages importants</h2>
        <% if (typeof messages !== 'undefined' && messages.length > 0) { %>
            <% messages.forEach(message => { %>
                <div class="message-card">
                    <p><%= message.text %></p>
                    <div class="message-date"><%= message.date %></div>
                </div>
            <% }); %>
        <% } else { %>
            <div>Aucun message</div>
        <% } %>
    </section>
</main>

<!-- Scripts -->
<script>
    // Ajouter des scripts ici si nécessaire
</script>
</body>
</html>
